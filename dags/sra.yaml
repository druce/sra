dag:
  version: 2
  name: Equity Research Report Bot
  vars:
    artifact_context: |
      Information gathered so far is in the artifacts/ subdirectory.
      Read artifacts/manifest.json for a description of all available files.
    artifact_context_inline: |
      Key artifacts are included inline below.
      Additional files are in artifacts/ — use Read tool for larger files not included inline.
    research_output_instructions: |
      Write all your findings to a single markdown document.
      Structure it with clear ## headings for major topics.
      Separate distinct findings with --- horizontal rules.
      Cite sources inline: (Source: Wikipedia), (Source: 10-K Item 1A), (Source: FMP company-profile), etc.
      Include specific data points: numbers, dates, percentages, dollar amounts.
      Each finding should be a self-contained paragraph that makes sense without surrounding context
      (the document will be split into chunks for a search index).
  inputs:
    ticker: "${ticker}"
    workdir: "${workdir}"
  root_dir: .
  template_dir: templates
  drafts_dir: drafts
  data_dir: data # not implemented, in future write reports based on user-provided data
  allowed_tools: all # not implemented, all allowed, in future limit tools

tasks:
  # --- Data gathering tasks ---
  profile:
    sort_order: 1
    description: Get company profile data based on symbol
    type: python
    config:
      script: skills/fetch_profile/fetch_profile.py
      args:
        ticker: "${ticker}"
        workdir: "${workdir}"
    outputs:
      profile:    {path: "artifacts/profile.json", format: json, description: "Company identity (name, sector, industry, country, employees), valuation snapshot (market cap, price, 52-week range, beta, shares outstanding)"}
    sets_vars:
      symbol:       {artifact: "artifacts/profile.json", key: "symbol"}
      company_name: {artifact: "artifacts/profile.json", key: "company_name"}

  peers:
    sort_order: 2
    description: Get company peers data based on symbol
    type: python
    config:
      script: skills/identify_peers/identify_peers.py
      args:
        ticker: "${ticker}"
        workdir: "${workdir}"
    outputs:
      peers_list: {path: "artifacts/peers_list.json", format: json, description: "Peer companies with symbol, name, price, and market cap; includes provider source and Claude-filtered flag"}

  technical:
    sort_order: 3
    description: Generate stock chart and technical indicators
    type: python
    depends_on: [profile, peers]
    config:
      script: skills/fetch_technical/fetch_technical.py
      args:
        ticker: "${ticker}"
        workdir: "${workdir}"
    outputs:
      chart:              {path: "artifacts/chart.png", format: png, description: "1-year stock price chart with SMA-50, SMA-200, volume bars, RSI subplot, and MACD subplot"}
      technical_analysis: {path: "artifacts/technical_analysis.json", format: json, description: "Technical indicators: SMA, EMA, RSI, MACD, Bollinger Bands, ATR, OBV, support/resistance levels, and trend signals"}

  fundamental:
    sort_order: 4
    description: Fetch financial statements, ratios, and analyst data
    type: python
    depends_on: [profile, peers]
    config:
      script: skills/fetch_fundamental/fetch_fundamental.py
      args:
        ticker: "${ticker}"
        workdir: "${workdir}"
        peers_file: "artifacts/peers_list.json"
    outputs:
      income_statement:        {path: "artifacts/income_statement.csv", format: csv, description: "Annual income statement: revenue, COGS, gross profit, operating income, net income, EPS, shares outstanding"}
      balance_sheet:           {path: "artifacts/balance_sheet.csv", format: csv, description: "Annual balance sheet: total assets, liabilities, equity, cash, debt, working capital, book value per share"}
      cash_flow:               {path: "artifacts/cash_flow.csv", format: csv, description: "Annual cash flow statement: operating, investing, financing cash flows, CapEx, free cash flow"}
      key_ratios:              {path: "artifacts/key_ratios.csv", format: csv, description: "Financial ratios for company and peers: P/E, P/B, P/S, EV/EBITDA, ROE, ROA, debt/equity, margins"}
      analyst_recommendations: {path: "artifacts/analyst_recommendations.json", format: json, description: "Wall Street analyst consensus: buy/hold/sell counts, price targets (low/mean/median/high), recent rating changes"}

  detailed_profile:
    sort_order: 5
    description: Research news, business profile, and executives via Claude
    type: python
    depends_on: [profile, peers]
    config:
      script: skills/fetch_detailed_profile_info/fetch_detailed_profile_info.py
      args:
        ticker: "${ticker}"
        workdir: "${workdir}"
    outputs:
      news_stories:       {path: "knowledge/news_stories.md", format: md, description: "Recent news stories and developments"}
      business_profile:   {path: "knowledge/business_profile.md", format: md, description: "Narrative business overview: history, products, markets, strategy, and competitive positioning"}
      executive_profiles: {path: "knowledge/executive_profiles.md", format: md, description: "Key executive bios: name, title, background, tenure, and notable achievements"}
      risk:               {path: "knowledge/risk_analysis.md", format: md, description: "Risk analysis: regulatory, operational, market, financial, and strategic risks"}
      investment_thesis:  {path: "knowledge/investment_thesis.md", format: md, description: "Investment thesis: investment rationale, catalysts, bull/bear cases, and valuation"}
      competitive:        {path: "knowledge/competitive_analysis.md", format: md, description: "Competitive landscape: positioning, market share, key competitors, and differentiation"}
      business_model:     {path: "knowledge/business_model_analysis.md", format: md, description: "Business model analysis: revenue streams, cost structure, unit economics, scalability, profitability, and moat / defensibility"}

  # perplexity:
  #   sort_order: 5
  #   description: Research news, business profile, and executives via Perplexity AI
  #   type: python
  #   depends_on: [profile, peers]
  #   config:
  #     script: skills/fetch_perplexity/fetch_perplexity.py
  #     args:
  #       ticker: "${ticker}"
  #       workdir: "${workdir}"
  #   outputs:
  #     news_stories:       {path: "artifacts/news_stories.md", format: md, description: "Recent news stories and developments sourced via Perplexity AI with citations"}
  #     business_profile:   {path: "artifacts/business_profile.md", format: md, description: "Narrative business overview: history, products, markets, strategy, and competitive positioning via Perplexity AI"}
  #     executive_profiles: {path: "artifacts/executive_profiles.md", format: md, description: "Key executive bios: name, title, background, tenure, and notable achievements via Perplexity AI"}

  fetch_edgar:
    sort_order: 6
    description: Fetch SEC filings (10-K, 10-Q, 8-K) via edgartools
    type: python
    depends_on: [profile, peers]
    config:
      script: skills/fetch_edgar/fetch_edgar.py
      args:
        ticker: "${ticker}"
        workdir: "${workdir}"
    outputs:
      filings_index: {path: "artifacts/filings_index.json", format: json, description: "Index of all fetched SEC filings with filing type, date, accession number, and local file paths"}
      10k_items:     {path: "knowledge/", format: md, description: "Extracted 10-K annual report sections: business overview, risk factors, MD&A, financial statements, and notes"}
      10q_items:     {path: "knowledge/", format: md, description: "Extracted 10-Q quarterly report sections: MD&A, financial statements, and risk factor updates"}
      financials:    {path: "artifacts/", format: csv, description: "SEC-sourced financial data tables extracted from XBRL filings"}
      8k_summary:    {path: "artifacts/8k_summary.json", format: json, description: "Recent 8-K current report summaries: material events, management changes, financial results announcements"}

  wikipedia:
    sort_order: 7
    description: Fetch Wikipedia company summary
    type: python
    depends_on: [profile, peers]
    config:
      script: skills/fetch_wikipedia/fetch_wikipedia.py
      args:
        ticker: "${ticker}"
        workdir: "${workdir}"
    outputs:
      wikipedia_summary: {path: "knowledge/wikipedia_summary.txt", format: txt, description: "Wikipedia article summary providing company history, operations, and public background"}
      wikipedia_full:    {path: "knowledge/wikipedia_full.txt", format: txt, description: "Full Wikipedia article text with complete detail on company history, operations, and background"}

  custom_research:
    sort_order: 8
    description: Run user-provided custom investigation prompts via Claude
    type: python
    depends_on: [profile, peers]
    config:
      script: skills/custom_research/custom_research.py
      args:
        ticker: "${ticker}"
        workdir: "${workdir}"
    outputs:
      custom_research_tags: {path: "artifacts/custom_research_tags.json", format: json, description: "Section relevance tags for each custom research response"}

  # perplexity_analysis:
  #   sort_order: 8
  #   description: Generate business model, competitive, risk, and investment analysis via Perplexity
  #   type: python
  #   depends_on: [profile, peers]
  #   config:
  #     script: skills/fetch_perplexity_analysis/fetch_perplexity_analysis.py
  #     args:
  #       ticker: "${ticker}"
  #       workdir: "${workdir}"
  #   outputs:
  #     business_model: {path: "artifacts/business_model_analysis.md", format: md, description: "Business model deep-dive: revenue streams, unit economics, scalability, and moat analysis via Perplexity AI"}
  #     competitive:    {path: "artifacts/competitive_analysis.md", format: md, description: "Competitive landscape: market share, key rivals, differentiation, and positioning analysis via Perplexity AI"}
  #     risk:           {path: "artifacts/risk_analysis.md", format: md, description: "Risk factor analysis: regulatory, market, operational, financial, and strategic risks via Perplexity AI"}
  #     investment:     {path: "artifacts/investment_thesis.md", format: md, description: "Investment thesis: bull/bear cases, catalysts, valuation considerations, and recommendation framework via Perplexity AI"}

  # --- Chunk, tag, and index wave ---
  # chunk docs and embed chunks in chunks.json
  chunk_documents:
    sort_order: 10
    description: Chunk and embed text artifacts for hybrid retrieval
    type: python
    depends_on: [technical, fundamental, detailed_profile, fetch_edgar, wikipedia, custom_research]
    config:
      script: skills/chunk_index/chunk_documents.py
      args:
        ticker: "${ticker}"
        workdir: "${workdir}"
    outputs:
      chunks: {path: "lancedb/chunks.json", format: json, description: "Text chunks with embeddings from all text artifacts"}

  # tag chunks with tags in chunk_tags.json
  tag_chunks:
    sort_order: 11
    description: Assign section relevance tags to each chunk using AI
    type: claude
    depends_on: [chunk_documents]
    config:
      prompt: |
        ${artifact_context}

        Read lancedb/chunks.json. For each chunk, assign relevance tags from this list:
        profile, business_model, competitive, supply_chain, financial, valuation, risk_news

        A chunk may have multiple tags if it is relevant to multiple sections.
        Apply tags generously — it is better to over-tag than under-tag.

        Write a JSON file to lancedb/chunk_tags.json in this exact format:
        [{"id": "<chunk_id>", "tags": ["tag1", "tag2"]}, ...]

        Include every chunk from chunks.json. Do not skip any.
      disallowed_tools: [WebSearch, WebFetch, yfinance, alphavantage, brave-search, wikipedia, openbb-mcp, playwright]
    outputs:
      chunk_tags: {path: "lancedb/chunk_tags.json", format: json, description: "Section relevance tags for each chunk"}

  # build LanceDB index from chunks and tags
  build_index:
    sort_order: 12
    description: Build LanceDB hybrid vector+BM25 index from chunks and tags
    type: python
    depends_on: [tag_chunks]
    config:
      script: skills/chunk_index/build_index.py
      args:
        ticker: "${ticker}"
        workdir: "${workdir}"
    outputs:
      index: {path: "lancedb/index/", format: lancedb, description: "LanceDB hybrid search index of all chunked artifacts"}

  # --- Research agent tasks (Claude subagents with MCP tools, run in parallel) ---
  research_profile:
    sort_order: 20
    description: Research company profile, history, and management
    type: claude
    depends_on: [build_index]
    config:
      mcp_config: [mcp-research.json]
      prompt: |
        You are a research analyst investigating ${company_name} (${symbol}).
        Your task is to gather and organize the raw material needed to write
        an extended narrative company profile including founding story, company
        history with key milestones and a business overview with recent developments.

        ${artifact_context}

        Ensure you have all key facts for a profile:
        - Founding date and founder(s)
        - Major historical milestones, mergers, acquisitions, and pivots
        - Current products and services, geographical and customer segments.
        - Business model and revenue streams
        - Financial metrics
        - Recent company news

        1. Retrieve everything already indexed for this section:
           Run: uv run python skills/search_index/search_index.py --all --workdir ${workdir} --sections profile
           Review these results carefully — this is what we already have from SEC filings, Wikipedia, and other ingested sources.

        2. Identify gaps in the existing material, then use MCP tools to fill them (company profile, executives, news, etc.).

        3. Read relevant structured artifacts in artifacts/ (JSON, CSV files) for specific data points.

        ${research_output_instructions}

        Save your findings to knowledge/findings_profile.md
      disallowed_tools: []
    outputs:
      findings: {path: "knowledge/findings_profile.md", format: md, description: "Research findings on company profile, history, and management"}

  research_business:
    sort_order: 21
    description: Research business model, revenue streams, and competitive moat
    type: claude
    depends_on: [build_index]
    config:
      mcp_config: [mcp-research.json]
      prompt: |
        ${artifact_context}

        You are a research analyst investigating ${company_name} (${symbol}).
        Your job is to gather and organize raw material — not to write the final analysis yet.
        Use the tools available to you to collect data for the section on the company's business model.

        1. Retrieve everything already indexed for this section:
           Run: uv run python skills/search_index/search_index.py --all --workdir ${workdir} --sections business_model
           Review these results carefully — this is what we already have from SEC filings, Wikipedia, and other ingested sources.

        2. Identify gaps in the existing material, then use MCP tools to research revenue breakdown, segment trends, and moat sources.

        Ensure you have all key facts for an analysis of the company's business model:
          - segment revenue breakdown (revenue by segment, $ and %)
          - geographic revenue breakdown ($ and %)
          - YoY growth rates for each segment and geography
          - named products and services, which segment they belong to, launch date if recent, and any disclosed pricing or subscription terms
          - any products discontinued or pivoted in the last 12 months
          - sales/distribution model (direct, dealer, online, channel partners)
          - revenue types (e.g., hardware sale, subscription, licensing, regulatory credits, services fees)
          - customer concentration (>10% customers)
          - Note any disclosed seasonality language from the 10-K MD&A
          - margins by segment
          - expenses like R&D, SG&A, COGS, attributable to segments or overhead
          - capital expenditures
          - growth drivers, forward guidance as to changes
          - key ratios and peer comparisons
          - key moats and competitive advantages
          - addressable market by segment
          - capital return strategies

          sources:
          - Use searches like "{COMPANY} new product launch {CURRENT_YEAR}" to ensure knowledge is complete
          - use all the docs, tools and market data services

        3. Read relevant structured artifacts in artifacts/ (JSON, CSV files) for specific data points.

        ${research_output_instructions}

        Save your findings to knowledge/findings_business_model.md
      disallowed_tools: []
    outputs:
      findings: {path: "knowledge/findings_business_model.md", format: md, description: "Research findings on business model, revenue streams, and competitive moat"}

  research_competitive:
    sort_order: 22
    description: Research competitive landscape, market share, and industry dynamics
    type: claude
    depends_on: [build_index]
    config:
      mcp_config: [mcp-research.json]
      prompt: |
        ${artifact_context}

        You are a research analyst investigating ${company_name} (${symbol}).
        Your job is to gather and organize raw material — not to write the final analysis yet.
        Use the tools available to you to collect data for the section on the company's competitive landscape, market share, industry dynamics, and positioning.

        1. Retrieve everything already indexed for this section:
           Run: uv run python skills/search_index/search_index.py --all --workdir ${workdir} --sections competitive
           Review these results carefully — this is what we already have from SEC filings, Wikipedia, and other ingested sources.

        2. Identify gaps in the existing material, then use MCP tools to research competitors, peer financials, and market dynamics.
        Ensure you have identified all key facts for an analysis of the company's competitive environment:
        - competitors, their value proposition and their market share
        - for significant compeitors
          - Scale: revenue, units sold, or relevant volume metric vs. the subject company
          - Specific competitive advantage or threat they pose (technology, cost, geography, product lineup etc.)
          - Recent developments
          - Any disadvantage, limitation or vulnerability relative to the subject company
        - emerging, adjacent, and potential competitors, analyze risk for disruption
        - trends in competitive environment, share, impact on profit margins
          - Where unit and dollar market share is being gained or lost to whom
          - Whether the losses are structural (product, brand, price) or cyclical
          - evidence of overall increase or decrease in competition/pricing power
          - areas of profit, investment and focus across peers

        3. Read relevant structured artifacts in artifacts/ (JSON, CSV files) for specific data points.

        ${research_output_instructions}

        Save your findings to knowledge/findings_competitive.md
      disallowed_tools: []
    outputs:
      findings: {path: "knowledge/findings_competitive.md", format: md, description: "Research findings on competitive landscape, market share, and industry dynamics"}

  research_supply_chain:
    sort_order: 23
    description: Research supply chain, manufacturing dependencies, and geopolitical exposure
    type: claude
    depends_on: [build_index]
    config:
      mcp_config: [mcp-research.json]
      prompt: |
        ${artifact_context}

        You are a research analyst investigating ${company_name} (${symbol}).
        Your domain: supply chain, manufacturing partners, input dependencies, and geopolitical exposure.

        1. Retrieve everything already indexed for this section:
           Run: uv run python skills/search_index/search_index.py --all --workdir ${workdir} --sections supply_chain
           Review these results carefully — this is what we already have from SEC filings, Wikipedia, and other ingested sources.

        2. Identify gaps in the existing material, then use MCP tools to research key suppliers, concentration risk, and geographic exposure.

        3. Read relevant structured artifacts in artifacts/ (JSON, CSV files) for specific data points.

        ${research_output_instructions}

        Save your findings to knowledge/findings_supply_chain.md
      disallowed_tools: []
    outputs:
      findings: {path: "knowledge/findings_supply_chain.md", format: md, description: "Research findings on supply chain, manufacturing, and geopolitical exposure"}

  research_financial:
    sort_order: 24
    description: Research financial performance, growth trends, and key metrics
    type: claude
    depends_on: [build_index]
    config:
      mcp_config: [mcp-research.json]
      prompt: |
        ${artifact_context}

        You are a research analyst investigating ${company_name} (${symbol}).
        Your domain: financial performance — revenue growth, margins, cash flow, balance sheet, and key ratios.

        1. Retrieve everything already indexed for this section:
           Run: uv run python skills/search_index/search_index.py --all --workdir ${workdir} --sections financial
           Review these results carefully — this is what we already have from SEC filings, financial data, and other ingested sources.

        2. Identify gaps in the existing material, then use MCP tools to research financial statements, TTM metrics, and trend analysis.

        3. Read relevant structured artifacts in artifacts/ (JSON, CSV files) for specific data points.

        ${research_output_instructions}

        Save your findings to knowledge/findings_financial.md
      disallowed_tools: []
    outputs:
      findings: {path: "knowledge/findings_financial.md", format: md, description: "Research findings on financial performance, growth trends, and key metrics"}

  research_valuation:
    sort_order: 25
    description: Research valuation multiples, analyst targets, and DCF inputs
    type: claude
    depends_on: [build_index]
    config:
      mcp_config: [mcp-research.json]
      prompt: |
        ${artifact_context}

        You are a research analyst investigating ${company_name} (${symbol}).
        Your domain: valuation — current multiples, historical ranges, analyst price targets, and DCF inputs.

        1. Retrieve everything already indexed for this section:
           Run: uv run python skills/search_index/search_index.py --all --workdir ${workdir} --sections valuation
           Review these results carefully — this is what we already have from financial data, analyst estimates, and other ingested sources.

        2. Identify gaps in the existing material, then use MCP tools to research current multiples, analyst consensus, and peer comparisons.

        3. Read relevant structured artifacts in artifacts/ (JSON, CSV files) for specific data points.

        ${research_output_instructions}

        Save your findings to knowledge/findings_valuation.md
      disallowed_tools: []
    outputs:
      findings: {path: "knowledge/findings_valuation.md", format: md, description: "Research findings on valuation multiples, analyst targets, and DCF inputs"}

  research_risk_news:
    sort_order: 26
    description: Research risks, recent news events, and regulatory developments
    type: claude
    depends_on: [build_index]
    config:
      mcp_config: [mcp-research.json]
      prompt: |
        ${artifact_context}

        You are a research analyst investigating ${company_name} (${symbol}).
        Your domain: key risks, recent news events, regulatory developments, and catalysts.

        1. Retrieve everything already indexed for this section:
           Run: uv run python skills/search_index/search_index.py --all --workdir ${workdir} --sections risk_news
           Review these results carefully — this is what we already have from SEC filings, news, and other ingested sources.

        2. Identify gaps in the existing material, then use MCP tools to research recent material events, risk factors, and regulatory filings.

        3. Read relevant structured artifacts in artifacts/ (JSON, CSV files) for specific data points.

        ${research_output_instructions}

        Save your findings to knowledge/findings_risk_news.md
      disallowed_tools: []
    outputs:
      findings: {path: "knowledge/findings_risk_news.md", format: md, description: "Research findings on risks, news events, and regulatory developments"}

  # --- Post-research: chunk, tag, and index research findings ---
  chunk_research:
    sort_order: 27
    description: Chunk and embed research findings for knowledge base
    type: python
    depends_on: [research_profile, research_business, research_competitive,
                 research_supply_chain, research_financial, research_valuation, research_risk_news]
    config:
      script: skills/chunk_index/chunk_research.py
      args:
        ticker: "${ticker}"
        workdir: "${workdir}"
    outputs:
      chunks: {path: "lancedb/research_chunks.json", format: json, description: "Chunked and embedded research findings"}

  tag_research:
    sort_order: 28
    description: Cross-tag research findings chunks with additional section relevance
    type: claude
    depends_on: [chunk_research]
    config:
      prompt: |
        Read lancedb/research_chunks.json. Each chunk already has a primary tag from its source file.
        Your job is to add ADDITIONAL cross-relevant tags only.

        Valid tags: profile, business_model, competitive, supply_chain, financial, valuation, risk_news

        For each chunk, decide if it is relevant to sections BEYOND its primary tag.
        Examples:
        - A profile finding about CEO resignation → also tag risk_news
        - A financial finding about margin compression → also tag competitive, valuation
        - A supply chain finding about tariff risk → also tag risk_news, financial

        Only add cross-tags when the content would genuinely help a writer in that other section.
        Do NOT remove existing tags. Output ALL tags (primary + any additions).

        Write to lancedb/research_chunk_tags.json in this exact format:
        [{"id": "<chunk_id>", "tags": ["primary_tag", "cross_tag_1", ...]}, ...]

        Include every chunk. Do not skip any.
      disallowed_tools: [WebSearch, WebFetch, yfinance, alphavantage, brave-search, wikipedia, openbb-mcp, playwright]
    outputs:
      chunk_tags: {path: "lancedb/research_chunk_tags.json", format: json, description: "Cross-tagged research findings chunks"}

  append_index:
    sort_order: 29
    description: Append tagged research findings to LanceDB knowledge base
    type: python
    depends_on: [tag_research]
    config:
      script: skills/chunk_index/append_index.py
      args:
        ticker: "${ticker}"
        workdir: "${workdir}"
    outputs:
      research_index: {path: "lancedb/index/", format: lancedb,
                       description: "LanceDB index enriched with cross-tagged research findings"}

  # --- Body section writing tasks (Claude subagents, run in parallel) ---
  write_profile:
    sort_order: 30
    description: "Write Section 2: Extended Profile"
    type: claude
    depends_on: [append_index]
    config:
      system: "You are a senior equity research analyst writing a professional report. Read and follow the style guide at ../../STYLE.md."
      disallowed_tools: [WebSearch, WebFetch, Agent, Skill, yfinance, alphavantage, brave-search, wikipedia, openbb-mcp, playwright, fetch, filesystem]
      artifacts_inline:
        - artifacts/manifest.json
        - artifacts/profile.json
        - artifacts/peers_list.json
        - artifacts/key_ratios.csv
        - artifacts/income_statement.csv
        - artifacts/balance_sheet.csv
        - artifacts/cash_flow.csv
        - artifacts/analyst_recommendations.json
        - artifacts/technical_analysis.json
        - artifacts/8k_summary.json
      prompt: |
        ${artifact_context_inline}

        Before writing, retrieve all research material tagged for this section:
          Run: uv run python skills/search_index/search_index.py --all --workdir ${workdir} --sections profile
        These are all indexed chunks relevant to this section — data artifacts, research findings, and MCP responses.
        Use these results as your primary source alongside the inline structured data above.
        Do not call external tools or APIs — focus on synthesis from provided data.

        ---

        I have collected comprehensive information on ${ticker} in the directory ${workdir}.

        IMPORTANT: Read the style guide at ../../STYLE.md before writing. Follow its formatting conventions, source reliability hierarchy, and naming conventions.

        All research data you need is provided in the inline artifacts above and in the artifacts/ directory.
        Use Read and Grep tools to access larger files not included inline.
        Do not attempt external research — synthesize from the provided data only.

        1 paragraph executive summary covering: what the company does, its scale (revenue, market cap,
        employees, geographic footprint), and what makes it an interesting from an investment perspective.

        ## History and Key Milestones
        1-3 paragraph narrative origin story: founding circumstances, founders, early vision. Then walk through the
        critical inflection points — pivots, product launches, IPO, major acquisitions, near-death
        experiences, and transformative moments — that explain how the company became what it is
        today. Emphasize events that shaped the current competitive position or business model.
        Include approximate dates.

        ## Core Business and Investment Highlights
          Cover in 1-3 paragraphs:
          - Revenue segments with approximate size and growth trajectory
          - Primary products/services and what differentiates them
          - Business model (how the company actually makes money)
          - Sources of competitive advantage (network effects, switching costs, IP, scale, distribution)
          - Key metrics investors track (unit economics, margins, retention, utilization, etc.)
          - The central tension or debate in the investment case (e.g., "priced as X, may become Y")

        ## Recent Developments
          Cover in 1 paragraph the most significant developments from the past 12 months that materially
          affect the investment case, such as: financial results, product launches, competitive moves,
          regulatory events, management changes, and any macro/policy tailwinds or headwinds.
          Be specific — include figures, dates, and context for why each development matters.
          Use a straightforward, factual tone throughout the analysis. Focus on data and observable facts rather than speculation.

        Save results in markdown format to drafts/section_2_profile.md
      n_iterations: 1
      critic_prompt: |
        Read the draft section at ${draft_path} carefully and critique it for an equity research report.

        Evaluate for:
        - Clarity and readability
        - Factual accuracy — cross-check claims against data in artifacts/
        - Missing or incomplete analysis
        - Unsupported claims or speculation
        - Repetition
        - Professional tone and formatting
        - Compliance with the style guide at ../../STYLE.md (formatting conventions, source hierarchy, naming)

        Write a numbered list of specific issues to address, ordered by importance.
        For each issue, explain what's wrong and suggest how to fix it.

        Save your critique to ${critique_path}.
      rewrite_prompt: |
        Read the current draft at ${draft_path} and the critique at ${critique_path}.

        Address each issue identified in the critique:
        - Improve clarity and readability
        - Remove repetition
        - Fix factual inaccuracies using data from artifacts/
        - Elaborate on incomplete analysis using existing artifacts
        - Remove or substantiate unsupported claims
        - Ensure professional tone
        - Follow the style guide at ../../STYLE.md

        Do not introduce new research. Use existing artifacts to resolve issues.
        If you cannot verify a claim from the artifacts, make the language more hedged or remove it.

        Save the revised section to ${rewrite_path}.
      critic_model: claude-sonnet-4-6
      rewrite_model: claude-sonnet-4-6
      critic_disallowed_tools: [WebSearch, WebFetch, Agent, Skill, yfinance, alphavantage, brave-search, wikipedia, openbb-mcp, playwright, fetch, filesystem]
      rewrite_disallowed_tools: [WebSearch, WebFetch, Agent, Skill, yfinance, alphavantage, brave-search, wikipedia, openbb-mcp, playwright, fetch, filesystem]
    outputs:
      section: {path: "artifacts/section_2_profile.md", format: md, description: "Section 2: Extended profile covering company history, core business, competitors, and recent news"}

  write_business_model:
    sort_order: 31
    description: "Write Section 3: Business Model"
    type: claude
    depends_on: [append_index]
    config:
      system: "You are a senior equity research analyst writing a professional report. Read and follow the style guide at ../../STYLE.md."
      disallowed_tools: [WebSearch, WebFetch, Agent, Skill, yfinance, alphavantage, brave-search, wikipedia, openbb-mcp, playwright, fetch, filesystem]
      artifacts_inline:
        - artifacts/manifest.json
        - artifacts/profile.json
        - artifacts/peers_list.json
        - artifacts/key_ratios.csv
        - artifacts/income_statement.csv
        - artifacts/balance_sheet.csv
        - artifacts/cash_flow.csv
        - artifacts/analyst_recommendations.json
        - artifacts/technical_analysis.json
        - artifacts/8k_summary.json
      prompt: |
        ${artifact_context_inline}

        Before writing, retrieve all research material tagged for this section:
          Run: uv run python skills/search_index/search_index.py --all --workdir ${workdir} --sections business_model
        These are all indexed chunks relevant to this section — data artifacts, research findings, and MCP responses.
        Use these results as your primary source alongside the inline structured data above.
        Do not call external tools or APIs — focus on synthesis from provided data.

        ---

        I have collected comprehensive information on ${ticker} in the directory ${workdir}.

        IMPORTANT: Read the style guide at ../../STYLE.md before writing. Follow its formatting conventions, source reliability hierarchy, and naming conventions.

        All research data you need is provided in the inline artifacts above and in the artifacts/ directory.
        Use Read and Grep tools to access larger files not included inline.
        Do not attempt external research — synthesize from the provided data only.

        Write Section 3 of an equity research report on ${ticker}: Business Model.

        Cover:
        • Core businesses, products and services.
        • Key revenue streams, customer segments, relative importances, and monetization strategies with data.
        • Recent changes, product launches, discontinuations, or pivots; nascent businesses or option-value segments
        • Analyze key monetization characteristics of the businesses it operates in:
          - model: one-time sale, subscription, b2b vs b2c etc.
          - customer types
          - customer acquisition and retention dynamics (CAC, sales cycles, switching costs, brand dynamics, distribution model)
          - seasonal or cyclical business patterns
          - margin structures and dynmaics by segment
          - market size, addressable size, current share, growth trajectory and factors affecting them

        • Explain sources of competitive advantage such as
          - network effects
          - switching costs
          - scale
          - distribution
          - vertical integration
          - exclusive relationships
          - branding
          - intellectual property
          - regulatory moats
          - and other barriers to entry.

        • Growth Drivers and Capital Allocation
          - Key organic growth vectors (new products, geographies, segments)
          - Capital expenditure level and stated priorities
          - Capital structure and expense structure vs. peers
          - Capital return policy (dividends, buybacks, dilution)
          - Any M&A strategy or recent transactions

        Use a straightforward, factual tone throughout the analysis.
        Focus on data, observable facts, and how they compare with competitors.
        Show trends in revenues, margins, and key financial metrics using tables.
        Analyse dynamics, margin trade-offs, cannibalization, concentration risks.

        Save results in markdown format to drafts/section_3_business_model.md
      n_iterations: 1
      critic_prompt: |
        Read the draft section at ${draft_path} carefully and critique it for an equity research report.

        Evaluate for:
        - Clarity and readability
        - Factual accuracy — cross-check claims against data in artifacts/
        - Missing or incomplete analysis
        - Unsupported claims or speculation
        - Repetition
        - Professional tone and formatting
        - Compliance with the style guide at ../../STYLE.md (formatting conventions, source hierarchy, naming)

        Write a numbered list of specific issues to address, ordered by importance.
        For each issue, explain what's wrong and suggest how to fix it.

        Save your critique to ${critique_path}.
      rewrite_prompt: |
        Read the current draft at ${draft_path} and the critique at ${critique_path}.

        Address each issue identified in the critique:
        - Improve clarity and readability
        - Remove repetition
        - Fix factual inaccuracies using data from artifacts/
        - Elaborate on incomplete analysis using existing artifacts
        - Remove or substantiate unsupported claims
        - Ensure professional tone
        - Follow the style guide at ../../STYLE.md

        Do not introduce new research. Use existing artifacts to resolve issues.
        If you cannot verify a claim from the artifacts, make the language more hedged or remove it.

        Save the revised section to ${rewrite_path}.
      critic_model: claude-sonnet-4-6
      rewrite_model: claude-sonnet-4-6
      critic_disallowed_tools: [WebSearch, WebFetch, Agent, Skill, yfinance, alphavantage, brave-search, wikipedia, openbb-mcp, playwright, fetch, filesystem]
      rewrite_disallowed_tools: [WebSearch, WebFetch, Agent, Skill, yfinance, alphavantage, brave-search, wikipedia, openbb-mcp, playwright, fetch, filesystem]
    outputs:
      section: {path: "artifacts/section_3_business_model.md", format: md, description: "Section 3: Business model analysis covering products, revenue streams, market characteristics, and competitive advantages"}

  write_competitive:
    sort_order: 32
    description: "Write Section 4: Competitive Landscape"
    type: claude
    depends_on: [append_index]
    config:
      system: "You are a senior equity research analyst writing a professional report. Read and follow the style guide at ../../STYLE.md."
      disallowed_tools: [WebSearch, WebFetch, Agent, Skill, yfinance, alphavantage, brave-search, wikipedia, openbb-mcp, playwright, fetch, filesystem]
      artifacts_inline:
        - artifacts/manifest.json
        - artifacts/profile.json
        - artifacts/peers_list.json
        - artifacts/key_ratios.csv
        - artifacts/income_statement.csv
        - artifacts/balance_sheet.csv
        - artifacts/cash_flow.csv
        - artifacts/analyst_recommendations.json
        - artifacts/technical_analysis.json
        - artifacts/8k_summary.json
      prompt: |
        ${artifact_context_inline}

        Before writing, retrieve all research material tagged for this section:
          Run: uv run python skills/search_index/search_index.py --all --workdir ${workdir} --sections competitive
        These are all indexed chunks relevant to this section — data artifacts, research findings, and MCP responses.
        Use these results as your primary source alongside the inline structured data above.
        Do not call external tools or APIs — focus on synthesis from provided data.

        ---

        I have collected comprehensive information on ${ticker} in the directory ${workdir}.

        IMPORTANT: Read the style guide at ../../STYLE.md before writing. Follow its formatting conventions, source reliability hierarchy, and naming conventions.

        All research data you need is provided in the inline artifacts above and in the artifacts/ directory.
        Use Read and Grep tools to access larger files not included inline.
        Do not attempt external research — synthesize from the provided data only.

        Write Section 4 of an equity research report on ${ticker}: Competitive Landscape.

        Cover:
        • Identify their main competitors, including direct, adjacent, and emerging competitors.
        • Compare key metrics such as market share, product differentiation, pricing power, and growth trajectories.

        Use a straightforward, factual tone throughout the analysis. Focus on data and observable facts rather than speculation.

        Save results in markdown format to drafts/section_4_competitive.md
      n_iterations: 1
      critic_prompt: |
        Read the draft section at ${draft_path} carefully and critique it for an equity research report.

        Evaluate for:
        - Clarity and readability
        - Factual accuracy — cross-check claims against data in artifacts/
        - Missing or incomplete analysis
        - Unsupported claims or speculation
        - Repetition
        - Professional tone and formatting
        - Compliance with the style guide at ../../STYLE.md (formatting conventions, source hierarchy, naming)

        Write a numbered list of specific issues to address, ordered by importance.
        For each issue, explain what's wrong and suggest how to fix it.

        Save your critique to ${critique_path}.
      rewrite_prompt: |
        Read the current draft at ${draft_path} and the critique at ${critique_path}.

        Address each issue identified in the critique:
        - Improve clarity and readability
        - Remove repetition
        - Fix factual inaccuracies using data from artifacts/
        - Elaborate on incomplete analysis using existing artifacts
        - Remove or substantiate unsupported claims
        - Ensure professional tone
        - Follow the style guide at ../../STYLE.md

        Do not introduce new research. Use existing artifacts to resolve issues.
        If you cannot verify a claim from the artifacts, make the language more hedged or remove it.

        Save the revised section to ${rewrite_path}.
      critic_model: claude-sonnet-4-6
      rewrite_model: claude-sonnet-4-6
      critic_disallowed_tools: [WebSearch, WebFetch, Agent, Skill, yfinance, alphavantage, brave-search, wikipedia, openbb-mcp, playwright, fetch, filesystem]
      rewrite_disallowed_tools: [WebSearch, WebFetch, Agent, Skill, yfinance, alphavantage, brave-search, wikipedia, openbb-mcp, playwright, fetch, filesystem]
    outputs:
      section: {path: "artifacts/section_4_competitive.md", format: md, description: "Section 4: Competitive landscape covering competitors, market share, differentiation, and growth trajectories"}

  write_supply_chain:
    sort_order: 33
    description: "Write Section 5: Supply Chain Positioning"
    type: claude
    depends_on: [append_index]
    config:
      system: "You are a senior equity research analyst writing a professional report. Read and follow the style guide at ../../STYLE.md."
      disallowed_tools: [WebSearch, WebFetch, Agent, Skill, yfinance, alphavantage, brave-search, wikipedia, openbb-mcp, playwright, fetch, filesystem]
      artifacts_inline:
        - artifacts/manifest.json
        - artifacts/profile.json
        - artifacts/peers_list.json
        - artifacts/key_ratios.csv
        - artifacts/income_statement.csv
        - artifacts/balance_sheet.csv
        - artifacts/cash_flow.csv
        - artifacts/analyst_recommendations.json
        - artifacts/technical_analysis.json
        - artifacts/8k_summary.json
      prompt: |
        ${artifact_context_inline}

        Before writing, retrieve all research material tagged for this section:
          Run: uv run python skills/search_index/search_index.py --all --workdir ${workdir} --sections supply_chain
        These are all indexed chunks relevant to this section — data artifacts, research findings, and MCP responses.
        Use these results as your primary source alongside the inline structured data above.
        Do not call external tools or APIs — focus on synthesis from provided data.

        ---

        I have collected comprehensive information on ${ticker} in the directory ${workdir}.

        IMPORTANT: Read the style guide at ../../STYLE.md before writing. Follow its formatting conventions, source reliability hierarchy, and naming conventions.

        All research data you need is provided in the inline artifacts above and in the artifacts/ directory.
        Use Read and Grep tools to access larger files not included inline.
        Do not attempt external research — synthesize from the provided data only.

        Write Section 5 of an equity research report on ${ticker}: Supply Chain Positioning.

        Cover:
        • Describe their role in the upstream (supplier-side) and downstream (customer/distribution) parts of the supply chain.
        • Identify key suppliers, partners, distributors, and any major dependencies or concentrations.

        Use a straightforward, factual tone throughout the analysis. Focus on data and observable facts rather than speculation.

        Save results in markdown format to drafts/section_5_supply_chain.md
      n_iterations: 1
      critic_prompt: |
        Read the draft section at ${draft_path} carefully and critique it for an equity research report.

        Evaluate for:
        - Clarity and readability
        - Factual accuracy — cross-check claims against data in artifacts/
        - Missing or incomplete analysis
        - Unsupported claims or speculation
        - Repetition
        - Professional tone and formatting
        - Compliance with the style guide at ../../STYLE.md (formatting conventions, source hierarchy, naming)

        Write a numbered list of specific issues to address, ordered by importance.
        For each issue, explain what's wrong and suggest how to fix it.

        Save your critique to ${critique_path}.
      rewrite_prompt: |
        Read the current draft at ${draft_path} and the critique at ${critique_path}.

        Address each issue identified in the critique:
        - Improve clarity and readability
        - Remove repetition
        - Fix factual inaccuracies using data from artifacts/
        - Elaborate on incomplete analysis using existing artifacts
        - Remove or substantiate unsupported claims
        - Ensure professional tone
        - Follow the style guide at ../../STYLE.md

        Do not introduce new research. Use existing artifacts to resolve issues.
        If you cannot verify a claim from the artifacts, make the language more hedged or remove it.

        Save the revised section to ${rewrite_path}.
      critic_model: claude-sonnet-4-6
      rewrite_model: claude-sonnet-4-6
      critic_disallowed_tools: [WebSearch, WebFetch, Agent, Skill, yfinance, alphavantage, brave-search, wikipedia, openbb-mcp, playwright, fetch, filesystem]
      rewrite_disallowed_tools: [WebSearch, WebFetch, Agent, Skill, yfinance, alphavantage, brave-search, wikipedia, openbb-mcp, playwright, fetch, filesystem]
    outputs:
      section: {path: "artifacts/section_5_supply_chain.md", format: md, description: "Section 5: Supply chain positioning covering upstream suppliers, downstream distribution, and key dependencies"}

  write_financial:
    sort_order: 34
    description: "Write Section 6: Financial and Operating Leverage"
    type: claude
    depends_on: [append_index]
    config:
      system: "You are a senior equity research analyst writing a professional report. Read and follow the style guide at ../../STYLE.md."
      disallowed_tools: [WebSearch, WebFetch, Agent, Skill, yfinance, alphavantage, brave-search, wikipedia, openbb-mcp, playwright, fetch, filesystem]
      artifacts_inline:
        - artifacts/manifest.json
        - artifacts/profile.json
        - artifacts/peers_list.json
        - artifacts/key_ratios.csv
        - artifacts/income_statement.csv
        - artifacts/balance_sheet.csv
        - artifacts/cash_flow.csv
        - artifacts/analyst_recommendations.json
        - artifacts/technical_analysis.json
        - artifacts/8k_summary.json
      prompt: |
        ${artifact_context_inline}

        Before writing, retrieve all research material tagged for this section:
          Run: uv run python skills/search_index/search_index.py --all --workdir ${workdir} --sections financial
        These are all indexed chunks relevant to this section — data artifacts, research findings, and MCP responses.
        Use these results as your primary source alongside the inline structured data above.
        Do not call external tools or APIs — focus on synthesis from provided data.

        ---

        I have collected comprehensive information on ${ticker} in the directory ${workdir}.

        IMPORTANT: Read the style guide at ../../STYLE.md before writing. Follow its formatting conventions, source reliability hierarchy, and naming conventions.

        All research data you need is provided in the inline artifacts above and in the artifacts/ directory.
        Use Read and Grep tools to access larger files not included inline.
        Do not attempt external research — synthesize from the provided data only.

        Write Section 6 of an equity research report on ${ticker}: Financial and Operating Leverage.

        Cover:
        • Analyze the company's use of financial leverage (debt levels, interest obligations, credit ratings).
        • Analyze operating leverage (fixed vs. variable cost structure, scalability, margin sensitivity to revenue changes).
        • Analyze cash flow generation and working capital dynamics.
        • Analyze capital allocation strategy (dividends, buybacks, reinvestment).

        Use a straightforward, factual tone throughout the analysis. Focus on data and observable facts rather than speculation.

        Save results in markdown format to drafts/section_6_financial.md
      n_iterations: 1
      critic_prompt: |
        Read the draft section at ${draft_path} carefully and critique it for an equity research report.

        Evaluate for:
        - Clarity and readability
        - Factual accuracy — cross-check claims against data in artifacts/
        - Missing or incomplete analysis
        - Unsupported claims or speculation
        - Repetition
        - Professional tone and formatting
        - Compliance with the style guide at ../../STYLE.md (formatting conventions, source hierarchy, naming)

        Write a numbered list of specific issues to address, ordered by importance.
        For each issue, explain what's wrong and suggest how to fix it.

        Save your critique to ${critique_path}.
      rewrite_prompt: |
        Read the current draft at ${draft_path} and the critique at ${critique_path}.

        Address each issue identified in the critique:
        - Improve clarity and readability
        - Remove repetition
        - Fix factual inaccuracies using data from artifacts/
        - Elaborate on incomplete analysis using existing artifacts
        - Remove or substantiate unsupported claims
        - Ensure professional tone
        - Follow the style guide at ../../STYLE.md

        Do not introduce new research. Use existing artifacts to resolve issues.
        If you cannot verify a claim from the artifacts, make the language more hedged or remove it.

        Save the revised section to ${rewrite_path}.
      critic_model: claude-sonnet-4-6
      rewrite_model: claude-sonnet-4-6
      critic_disallowed_tools: [WebSearch, WebFetch, Agent, Skill, yfinance, alphavantage, brave-search, wikipedia, openbb-mcp, playwright, fetch, filesystem]
      rewrite_disallowed_tools: [WebSearch, WebFetch, Agent, Skill, yfinance, alphavantage, brave-search, wikipedia, openbb-mcp, playwright, fetch, filesystem]
    outputs:
      section: {path: "artifacts/section_6_financial.md", format: md, description: "Section 6: Financial and operating leverage covering debt, margins, cash flow, and capital allocation"}

  write_valuation:
    sort_order: 35
    description: "Write Section 7: Valuation"
    type: claude
    depends_on: [append_index]
    config:
      system: "You are a senior equity research analyst writing a professional report. Read and follow the style guide at ../../STYLE.md."
      disallowed_tools: [WebSearch, WebFetch, Agent, Skill, yfinance, alphavantage, brave-search, wikipedia, openbb-mcp, playwright, fetch, filesystem]
      artifacts_inline:
        - artifacts/manifest.json
        - artifacts/profile.json
        - artifacts/peers_list.json
        - artifacts/key_ratios.csv
        - artifacts/income_statement.csv
        - artifacts/balance_sheet.csv
        - artifacts/cash_flow.csv
        - artifacts/analyst_recommendations.json
        - artifacts/technical_analysis.json
        - artifacts/8k_summary.json
      prompt: |
        ${artifact_context_inline}

        Before writing, retrieve all research material tagged for this section:
          Run: uv run python skills/search_index/search_index.py --all --workdir ${workdir} --sections valuation
        These are all indexed chunks relevant to this section — data artifacts, research findings, and MCP responses.
        Use these results as your primary source alongside the inline structured data above.
        Do not call external tools or APIs — focus on synthesis from provided data.

        ---

        I have collected comprehensive information on ${ticker} in the directory ${workdir}.

        IMPORTANT: Read the style guide at ../../STYLE.md before writing. Follow its formatting conventions, source reliability hierarchy, and naming conventions.

        All research data you need is provided in the inline artifacts above and in the artifacts/ directory.
        Use Read and Grep tools to access larger files not included inline.
        Do not attempt external research — synthesize from the provided data only.

        Write Section 7 of an equity research report on ${ticker}: Valuation.

        Cover:
        • Identify appropriate valuation methodologies, including income-based (e.g., DCF), asset-based (e.g. book value and sum of parts), market-based (e.g. peer multiples and comparisons), and LBO analysis
        • Highlight important valuation inputs and metrics (growth rates, margins, discount rates, terminal value assumptions).
        • Summarize current ratings and analyst opinions, including recent changes. Note if the stock is widely covered and whether opinions are polarized or clustered around a consensus.
        • Note the stock's volatility, liquidity, if it is widely covered and owned, if it is a hedge fund story stock or meme stock, what macro factors it is sensitive to

        Use a straightforward, factual tone throughout the analysis. Focus on data and observable facts rather than speculation.

        Save results in markdown format to drafts/section_7_valuation.md
      n_iterations: 1
      critic_prompt: |
        Read the draft section at ${draft_path} carefully and critique it for an equity research report.

        Evaluate for:
        - Clarity and readability
        - Factual accuracy — cross-check claims against data in artifacts/
        - Missing or incomplete analysis
        - Unsupported claims or speculation
        - Repetition
        - Professional tone and formatting
        - Compliance with the style guide at ../../STYLE.md (formatting conventions, source hierarchy, naming)

        Write a numbered list of specific issues to address, ordered by importance.
        For each issue, explain what's wrong and suggest how to fix it.

        Save your critique to ${critique_path}.
      rewrite_prompt: |
        Read the current draft at ${draft_path} and the critique at ${critique_path}.

        Address each issue identified in the critique:
        - Improve clarity and readability
        - Remove repetition
        - Fix factual inaccuracies using data from artifacts/
        - Elaborate on incomplete analysis using existing artifacts
        - Remove or substantiate unsupported claims
        - Ensure professional tone
        - Follow the style guide at ../../STYLE.md

        Do not introduce new research. Use existing artifacts to resolve issues.
        If you cannot verify a claim from the artifacts, make the language more hedged or remove it.

        Save the revised section to ${rewrite_path}.
      critic_model: claude-sonnet-4-6
      rewrite_model: claude-sonnet-4-6
      critic_disallowed_tools: [WebSearch, WebFetch, Agent, Skill, yfinance, alphavantage, brave-search, wikipedia, openbb-mcp, playwright, fetch, filesystem]
      rewrite_disallowed_tools: [WebSearch, WebFetch, Agent, Skill, yfinance, alphavantage, brave-search, wikipedia, openbb-mcp, playwright, fetch, filesystem]
    outputs:
      section: {path: "artifacts/section_7_valuation.md", format: md, description: "Section 7: Valuation analysis covering DCF, peer multiples, analyst ratings, and market characteristics"}

  write_risk_news:
    sort_order: 36
    description: "Write Section 8: Recent Developments, News Search and Risk Factors"
    type: claude
    depends_on: [append_index]
    config:
      system: "You are a senior equity research analyst writing a professional report. Read and follow the style guide at ../../STYLE.md."
      disallowed_tools: [WebSearch, WebFetch, Agent, Skill, yfinance, alphavantage, brave-search, wikipedia, openbb-mcp, playwright, fetch, filesystem]
      artifacts_inline:
        - artifacts/manifest.json
        - artifacts/profile.json
        - artifacts/peers_list.json
        - artifacts/key_ratios.csv
        - artifacts/income_statement.csv
        - artifacts/balance_sheet.csv
        - artifacts/cash_flow.csv
        - artifacts/analyst_recommendations.json
        - artifacts/technical_analysis.json
        - artifacts/8k_summary.json
      prompt: |
        ${artifact_context_inline}

        Before writing, retrieve all research material tagged for this section:
          Run: uv run python skills/search_index/search_index.py --all --workdir ${workdir} --sections risk_news
        These are all indexed chunks relevant to this section — data artifacts, research findings, and MCP responses.
        Use these results as your primary source alongside the inline structured data above.
        Do not call external tools or APIs — focus on synthesis from provided data.

        ---

        I have collected comprehensive information on ${ticker} in the directory ${workdir}.

        IMPORTANT: Read the style guide at ../../STYLE.md before writing. Follow its formatting conventions, source reliability hierarchy, and naming conventions.

        All research data you need is provided in the inline artifacts above and in the artifacts/ directory.
        Use Read and Grep tools to access larger files not included inline.
        Do not attempt external research — synthesize from the provided data only.

        Write Section 8 of an equity research report on ${ticker}: Recent Developments, News Search and Risk Factors.

        Use the comprehensive research data available. Key sources are in the LanceDB index (query via search_index.py above).
        Structured data is inline above. Text sources (news stories, risk analysis, investment thesis, SEC filings)
        are indexed in LanceDB and available via search.

        Synthesize these sources into a comprehensive section covering:
        • Recent corporate developments and their significance
        • Regulatory actions, legal proceedings, and compliance
        • Strategic partnerships, M&A activity, competitive dynamics
        • Analyst sentiment shifts and institutional ownership changes
        • Key risk factors by category (operational, financial, regulatory, market)

        Use a straightforward, factual tone throughout the analysis. Focus on data and observable facts rather than speculation.

        Save results in markdown format to drafts/section_8_risk_news.md
      n_iterations: 1
      critic_prompt: |
        Read the draft section at ${draft_path} carefully and critique it for an equity research report.

        Evaluate for:
        - Clarity and readability
        - Factual accuracy — cross-check claims against data in artifacts/
        - Missing or incomplete analysis
        - Unsupported claims or speculation
        - Repetition
        - Professional tone and formatting
        - Compliance with the style guide at ../../STYLE.md (formatting conventions, source hierarchy, naming)

        Write a numbered list of specific issues to address, ordered by importance.
        For each issue, explain what's wrong and suggest how to fix it.

        Save your critique to ${critique_path}.
      rewrite_prompt: |
        Read the current draft at ${draft_path} and the critique at ${critique_path}.

        Address each issue identified in the critique:
        - Improve clarity and readability
        - Remove repetition
        - Fix factual inaccuracies using data from artifacts/
        - Elaborate on incomplete analysis using existing artifacts
        - Remove or substantiate unsupported claims
        - Ensure professional tone
        - Follow the style guide at ../../STYLE.md

        Do not introduce new research. Use existing artifacts to resolve issues.
        If you cannot verify a claim from the artifacts, make the language more hedged or remove it.

        Save the revised section to ${rewrite_path}.
      critic_model: claude-sonnet-4-6
      rewrite_model: claude-sonnet-4-6
      critic_disallowed_tools: [WebSearch, WebFetch, Agent, Skill, yfinance, alphavantage, brave-search, wikipedia, openbb-mcp, playwright, fetch, filesystem]
      rewrite_disallowed_tools: [WebSearch, WebFetch, Agent, Skill, yfinance, alphavantage, brave-search, wikipedia, openbb-mcp, playwright, fetch, filesystem]
    outputs:
      section: {path: "artifacts/section_8_risk_news.md", format: md, description: "Section 8: Recent developments, news, and risk factors from deep research"}

  assemble_body:
    sort_order: 37
    description: Assemble the 7 body sections into a single report body
    type: python
    depends_on: [write_profile, write_business_model, write_competitive, write_supply_chain, write_financial, write_valuation, write_risk_news]
    config:
      script: skills/render_template.py
      args:
        template: templates/assemble_body.md.j2
        output: "${workdir}/artifacts/assembled_body.md"
        file: "section_2=${workdir}/artifacts/section_2_profile.md section_3=${workdir}/artifacts/section_3_business_model.md section_4=${workdir}/artifacts/section_4_competitive.md section_5=${workdir}/artifacts/section_5_supply_chain.md section_6=${workdir}/artifacts/section_6_financial.md section_7=${workdir}/artifacts/section_7_valuation.md section_8=${workdir}/artifacts/section_8_risk_news.md"
    outputs:
      body_text: {path: "artifacts/assembled_body.md", format: md, description: "Assembled report body combining all 7 sections (2-8) into a single document"}

  write_conclusion:
    sort_order: 38
    description: Write the main report conclusion
    type: claude
    depends_on: [assemble_body]
    config:
      system: "You are a senior equity research analyst writing a professional report. Read and follow the style guide at ../../STYLE.md."
      disallowed_tools: [WebSearch, WebFetch, Agent, Skill, Bash, yfinance, alphavantage, brave-search, wikipedia, openbb-mcp, playwright, fetch, filesystem]
      artifacts_inline:
        - artifacts/manifest.json
        - artifacts/profile.json
        - artifacts/peers_list.json
        - artifacts/key_ratios.csv
        - artifacts/analyst_recommendations.json
        - artifacts/technical_analysis.json
      prompt: |
        ${artifact_context_inline}

        read the report body at artifacts/assembled_body.md carefully and write a concluding 1-3 short paragraphs < 500 words total.
            • Summarize the company's strategic position and the stock's investment risk/reward profile
            • Do not repeat at length but draw on the most salient points from the report body: SWOT, bull/bear cases, risk assessment, and monitoring watchpoints.

        Use a straightforward, factual tone throughout the analysis. Focus on data and observable facts rather than speculation.

        Save results in markdown format to artifacts/conclusion.md.
    outputs:
      section: {path: "artifacts/conclusion.md", format: md, description: "Report conclusion: SWOT analysis, bull/bear cases, risk assessment, and monitoring watchpoints"}

  write_intro:
    sort_order: 39
    description: Write the main report intro
    type: claude
    depends_on: [assemble_body, write_conclusion]
    config:
      system: "You are a senior equity research analyst writing a professional report. Read and follow the style guide at ../../STYLE.md."
      disallowed_tools: [WebSearch, WebFetch, Agent, Skill, Bash, yfinance, alphavantage, brave-search, wikipedia, openbb-mcp, playwright, fetch, filesystem]
      artifacts_inline:
        - artifacts/manifest.json
        - artifacts/profile.json
      prompt: |
        ${artifact_context_inline}

        read the report body at artifacts/assembled_body.md and the conclusion at artifacts/conclusion.md carefully, then write a short introductory paragraph < 100 words.

        Use a straightforward, factual tone throughout the analysis.

        Save results in markdown format to artifacts/intro.md.
    outputs:
      section: {path: "artifacts/intro.md", format: md, description: "Short introductory paragraph (<100 words) summarizing the report scope and key findings"}

  assemble_text:
    sort_order: 40
    description: Assemble the 3 text sections into a complete report
    type: python
    depends_on: [assemble_body,write_conclusion, write_intro]
    config:
      script: skills/render_template.py
      args:
        template: templates/assemble_report.md.j2
        output: "${workdir}/artifacts/report_body.md"
        json: "${workdir}/artifacts/profile.json"
        file: "intro=${workdir}/artifacts/intro.md body=${workdir}/artifacts/assembled_body.md conclusion=${workdir}/artifacts/conclusion.md"
    outputs:
      body_text: {path: "artifacts/report_body.md", format: md, description: "Assembled full report: intro + body sections 2-8 + conclusion in final markdown format"}

  critique_body_final:
    sort_order: 41
    description: Critique the assembled report and identify issues to address
    type: claude
    depends_on: [assemble_text]
    config:
      system: "You are a senior equity research editor with expertise in financial analysis and report quality. Read and follow the style guide at ../../STYLE.md."
      model: claude-sonnet-4-6
      max_budget_usd: 1.00
      disallowed_tools: [WebSearch, WebFetch, Agent, Skill, Bash, yfinance, alphavantage, brave-search, wikipedia, openbb-mcp, playwright, fetch, filesystem]
      artifacts_inline:
        - artifacts/manifest.json
        - artifacts/profile.json
        - artifacts/key_ratios.csv
        - artifacts/analyst_recommendations.json
        - artifacts/technical_analysis.json
        - artifacts/8k_summary.json
      prompt: |
        ${artifact_context_inline}

        Read the assembled report text at artifacts/report_body.md carefully and provide a detailed critique.

        Evaluate the report for:
        - Following the structure exactly, with these exact numbered headings and no missing sections
          - 1. Introduction
          - 2. Company Profile
          - 3. Business Model
          - 4. Competitive Landscape
          - 5. Supply Chain Positioning
          - 6. Financial Profile
          - 7. Risks
          - 8. Investment Thesis and Conclusion
        - Clarity and readability
        - Repetition; similar details and points should not be repeated in different sections. As an example, early summary sections and later concluding sections can REFER to e.g. supply chain issues but SHOULD NOT detail at length the same issues discussed comprehensively in the supply chain part of the report.
        - Factual accuracy and consistency
        - Missing or incomplete analysis
        - Logical flow
        - Data quality and sourcing
        - Professional tone and formatting
        - Unsupported claims or speculation
        - Areas needing additional research or verification
        - Compliance with the style guide at ../../STYLE.md (formatting conventions, source hierarchy, naming)

        Write a numbered list of specific issues to address, ordered by importance.
        For each issue, explain what's wrong and suggest how to fix it.

        Save the critique to artifacts/report_critique.md
    outputs:
      critique: {path: "artifacts/report_critique.md", format: md, description: "Numbered list of issues identified in the report with explanations and suggested fixes"}

  polish_body_final:
    sort_order: 42
    description: Revise the report based on critique feedback
    type: claude
    depends_on: [critique_body_final]
    config:
      system: "You are a senior equity research analyst revising a report based on editorial feedback. Read and follow the style guide at ../../STYLE.md."
      model: claude-sonnet-4-6
      max_budget_usd: 1.00
      disallowed_tools: [WebSearch, WebFetch, Agent, Skill, Bash, yfinance, alphavantage, brave-search, wikipedia, openbb-mcp, playwright, fetch, filesystem]
      artifacts_inline:
        - artifacts/manifest.json
        - artifacts/profile.json
        - artifacts/key_ratios.csv
        - artifacts/analyst_recommendations.json
        - artifacts/technical_analysis.json
        - artifacts/8k_summary.json
      prompt: |
        ${artifact_context_inline}

        Read the critique at artifacts/report_critique.md and the current report text at artifacts/report_body.md.

        Carefully address each issue identified in the critique:
        - Improve clarity and readability
        - Remove repetition and duplication. If you find the same information repeated, keep only the best version, update it with any useful information from other versions, and replace other versions with a reference like "as detailed in Section X".
        - Fix factual inaccuracies or inconsistencies. Attempt to verify in the available artifacts. Delete statements or make them more general rather than embarking on new research.
        - Elaborate on missing or incomplete analysis. Again, use existing artifacts rather than going down a new rabbit hole.
        - Improve logical flow and structure
        - Enhance data quality and sourcing citations
        - Ensure professional tone and formatting
        - Remove or substantiate any unsupported claims using provided artifacts.

        Do not introduce new information or conduct new research. Resolve using the provided artifacts.. If there is a contradiction between the critique and the artifacts, trust the artifacts. If there is a contradictin between artifacts, note the contradiction.

        IMPORTANT:
          - this report will be assembled with a header including title, charts, technical analysis, peer comparison table. Return only the body text starting at section 1, Introduction, without any title or header.

        Save the revised report to artifacts/report_body_final.md.
    outputs:
      final_report: {path: "artifacts/report_body_final.md", format: md, description: "Final polished report incorporating all critique feedback and revisions"}

  final_assembly:
    sort_order: 43
    description: Assemble all report components into final formatted report using Jinja2 template
    type: python
    depends_on: [polish_body_final]
    config:
      script: skills/render_final.py
      args:
        workdir: "${workdir}"
    outputs:
      final_report: {path: "artifacts/final_report.md", format: md, description: "Complete formatted report with title, charts, tables, and polished body content assembled via Jinja2 template"}
