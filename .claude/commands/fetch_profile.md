---
description: Fetch company profile and peer companies for a ticker
---

# Run the fetch_profile skill for the given ticker

**Arguments:** $ARGUMENTS (expects: SYMBOL [--workdir DIR] [--peers SYM1,SYM2,...] [--no-filter-peers])

```bash
uv run ./skills/fetch_profile/fetch_profile.py $ARGUMENTS
```

Execute the command above. Report the results: what artifacts were produced, any errors or warnings, and the JSON manifest output.
