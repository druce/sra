---
description: Generate stock chart and technical indicators for a ticker
---

# Run the fetch_technical skill for the given ticker

**Arguments:** $ARGUMENTS (expects: SYMBOL [--workdir DIR])

```bash
uv run ./skills/fetch_technical/fetch_technical.py $ARGUMENTS
```

Execute the command above. Report the results: what artifacts were produced, any errors or warnings, and the JSON manifest output.
